# 仓库规范

## 部署方案

该项目将作为静态网站放上帽子云，因此使用非html、css、js、react技术的时候，应该考虑该技术是否可以在帽子云上有效接入，并且考虑访问速度

## 页面UI设计要求

每个组件与数据之间，应该有适当的边距，UI设计应当是平面简洁风格，参照discord网页，尽量减少线框和发光的使用。通过调整标题字体的大小来使内容结构清晰明了。
即使我给出了参考网页，但是我给出的数据中没有的内容，不要强行加进去。

## 项目结构

当前项目为教程页面，页面布局应该简洁明了

## 数据修改规范

所有文本文件必须使用 UTF-8 编码。

## 开发约束

需要安装的编码环境统一放在 `D:\\Code\\env`，编码工具统一放在 `D:\\Code\\build`。实现需求前先讨论详细方案；未经明确许可不得新增接口，非必要不增加接口。后续页面和工具开发应优先保证性能，并采用现代、清晰且易用的界面设计。
